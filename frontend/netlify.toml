[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so React Router deep links (e.g. /chat, /admin/dashboard) work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
